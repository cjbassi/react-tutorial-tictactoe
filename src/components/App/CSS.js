import styled from 'styled-components'

const Game = styled.div`
  display: flex;
  flex-direction: row;
  margin: 20px;
`

const GameInfo = styled.div`
  margin-left: -30px;
  padding-left: 0px;
`

export {
  Game,
  GameInfo,
}
